{
  "name": "@magallanes/api-flights",
  "version": "0.0.1",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  }
}
