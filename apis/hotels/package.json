{
  "name": "@magallanes/api-hotels",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.json",
    "build": "npm run clean && npm run compile",
    "start": "node dist/index.js"
  }
}
